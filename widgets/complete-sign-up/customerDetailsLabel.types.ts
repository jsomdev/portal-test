import { IImageStyles } from '@fluentui/react';

export interface CustomerDetailsLabelProps {
  labelText: string;
  modalTitle: string;
  modalText: string;
  modalImage: string;
}

export interface CustomerDetailsLabelStyles {
  image: IImageStyles;
}
