import { IButtonStyles, IStackStyles, ITextStyles } from '@fluentui/react';

export interface CheckoutFormOverviewGroupContainerStyles {
  label: ITextStyles;
  editButton: IButtonStyles;
  container: IStackStyles;
}
