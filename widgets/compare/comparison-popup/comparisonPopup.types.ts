import { IButtonStyles, IStackStyles } from '@fluentui/react';

export interface ComparisonPopupStyles {
  popupWrapper: IStackStyles;
  toggleButton: IButtonStyles;
  listWrapper: IStackStyles;
  compareButton: IButtonStyles;
  compareWrapper: IStackStyles;
}
